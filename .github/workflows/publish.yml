name: Publish Package

on:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  publish:
    uses: think-herrmann/axon-core/.github/workflows/shared-publish-npm.yml@development
    with:
      package_name: '@think-herrmann/react-orgchart'
      build_command: 'npm run build'
      node_version: '20.x'
